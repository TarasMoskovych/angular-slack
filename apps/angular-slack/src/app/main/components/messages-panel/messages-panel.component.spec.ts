import { MessagesPanelComponent } from './messages-panel.component';

describe('MessagesPanelComponent', () => {
  let component: MessagesPanelComponent;

  beforeEach(() => {
    component = new MessagesPanelComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
