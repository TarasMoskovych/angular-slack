import { SidePanelComponent } from './side-panel.component';

describe('SidePanelComponent', () => {
  let component: SidePanelComponent;

  beforeEach(() => {
    component = new SidePanelComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
