import { ThemePanelComponent } from './theme-panel.component';

describe('ThemePanelComponent', () => {
  let component: ThemePanelComponent;

  beforeEach(() => {
    component = new ThemePanelComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
