import { ThemesPanelComponent } from './themes-panel.component';

describe('ThemesPanelComponent', () => {
  let component: ThemesPanelComponent;

  beforeEach(() => {
    component = new ThemesPanelComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
