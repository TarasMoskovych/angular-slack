import { ThemesComponent } from './themes.component';

describe('ThemesComponent', () => {
  let component: ThemesComponent;

  beforeEach(() => {
    component = new ThemesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
